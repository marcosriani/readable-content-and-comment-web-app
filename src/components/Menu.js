import React from 'react';
import './Menu.css';

const Menu = () => {
  return (
    <div className='container-menu'>
      <button className='btn-menu'>HOME</button>{' '}
      <button className='btn-menu'>Other</button>
    </div>
  );
};

export default Menu;
